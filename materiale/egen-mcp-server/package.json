{
  "name": "mcp-study-tasks",
  "version": "1.0.0",
  "description": "En lille MCP-server til undervisning i resources, tools og prompts",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "inspect": "npx -y @modelcontextprotocol/inspector@latest node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/taskStore.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  }
}
